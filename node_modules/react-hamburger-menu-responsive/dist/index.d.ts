import React from 'react';

declare function HamburgerMenu({ barsColor, closeColor, trigger, setTrigger, backgroundColor, children, childrenStyle, breakPoint, }: {
    barsColor?: string;
    closeColor?: string;
    trigger: boolean;
    setTrigger: React.Dispatch<React.SetStateAction<boolean>>;
    backgroundColor?: string;
    children?: React.ReactNode;
    childrenStyle?: React.CSSProperties;
    breakPoint?: number;
}): React.JSX.Element;

export { HamburgerMenu };
